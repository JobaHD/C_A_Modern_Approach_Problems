#include <ctype.h>
#include <stdio.h>

int censor(char *str, int n) {
    char *currChar = str;
    while (currChar < str + n) {
        if (tolower(*currChar) == "

}

int main(int argc, char **argv) {
}
